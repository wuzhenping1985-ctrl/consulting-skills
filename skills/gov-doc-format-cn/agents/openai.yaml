interface:
  display_name: "党政公文格式"
  short_description: "将 Word 或 PDF 编排为规范党政公文 Word"
  default_prompt: "Use $gov-doc-format-cn to convert the attached Word or PDF into a DOCX formatted as a Chinese party/government official document."

policy:
  allow_implicit_invocation: true
